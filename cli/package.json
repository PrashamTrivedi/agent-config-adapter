{
  "name": "aca-cli",
  "version": "1.0.0",
  "description": "CLI sync tool for Agent Config Adapter",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
