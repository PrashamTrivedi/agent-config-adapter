# Anthropic API Key for Claude Code
# Get your API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Override host service URLs if needed
# These are examples - only needed if you want to customize
# HOST_API_URL=http://host.docker.internal:8787
# HOST_DB_URL=postgresql://user:pass@host.docker.internal:5432/dbname
